{
  "name": "iumrah-hotels-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --check src/hotel-price.js && node --check src/hotel-price-source.js",
    "deploy": "wrangler deploy --config wrangler.generated.jsonc",
    "migrate": "wrangler d1 migrations apply HOTELS_DB --remote --config wrangler.generated.jsonc",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "1.1.0"
  }
}
